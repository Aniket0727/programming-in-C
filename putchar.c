#include<stdio.h>
#include<conio.h>

int main()
{
    int c;
    printf("Enter Char: ");
    c=getchar();
    // print("/n %c",c);
    putchar(c);
    putch(c);
    puts(c);


    return 0;
}

// putchar() and putch()
// Usage: Outputs a single character to the standard output (usually the screen).

// puts():
// Usage: Outputs a string to the standard output followed by a newline character.