#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5}; // Static array with fixed size of 5

    // Accessing and printing elements
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
